name: Deploy to Chrome Web Store
on: [push]
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build
        run: npm ci

      - name: Zip 
        uses: zip -r dist.zip dist